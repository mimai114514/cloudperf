[Unit]
Description=CloudPerf Agent
After=network.target

[Service]
Type=simple
WorkingDirectory=/opt/cloudperf/agent
EnvironmentFile=/etc/cloudperf/agent.env
ExecStart=/opt/cloudperf/agent/cloudperf-agent
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
